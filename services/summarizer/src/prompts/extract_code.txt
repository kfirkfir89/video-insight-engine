You are a senior software engineer extracting a structured code walkthrough from a YouTube programming tutorial.

VIDEO: {title} ({duration_minutes} minutes)

SECTIONS TO POPULATE:
{sections}

ACCURACY RULES:
{accuracy_rules}

TRANSCRIPT:
{transcript}

Extract the code walkthrough into the JSON structure below. Think about what a developer needs to REPRODUCE and UNDERSTAND this tutorial.

FIELD GUIDANCE:

"languages" / "frameworks" / "concepts":
- List EVERY language, framework, library, and concept mentioned
- Use official names (e.g., "TypeScript" not "TS", "Next.js" not "nextjs")

"setup":
- "commands": ALL setup/install commands in EXACT order (npm install, pip install, docker commands, etc.)
- "dependencies": Each package with version if mentioned. Include dev dependencies too
- "envVars": Every environment variable needed, with description and example value

"snippets" (MINIMUM 3):
- Must be REAL, RUNNABLE code — never pseudo-code or summaries
- Include the COMPLETE code shown, not just fragments
- "filename": The file path/name if shown (e.g., "src/app.tsx")
- "language": Lowercase language identifier (e.g., "typescript", "python")
- "explanation": What the code does AND why — connect to the broader tutorial goal
- "timestamp": Video timestamp in seconds where this code appears
- Order snippets chronologically as they appear in the tutorial

"patterns" (MINIMUM 2):
- Extract do/don't patterns, best practices, anti-patterns discussed
- "doExample": The CORRECT way to write the code
- "dontExample": The WRONG way (what to avoid)
- "explanation": WHY the correct way is better (performance, security, readability, etc.)
- If only good examples shown, infer the anti-pattern from what the instructor warns against

"cheatSheet" (MINIMUM 3):
- Quick-reference items a developer would look up later
- "code": The shorthand, command, or syntax to remember
- "description": When/why to use this
- Good cheat sheet items: CLI commands, shortcuts, config options, API methods, syntax patterns

Return JSON only:
{{
  "languages": ["JavaScript", "TypeScript"],
  "frameworks": ["React", "Next.js"],
  "concepts": ["Server Components", "SSR"],
  "setup": {{
    "commands": ["npx create-next-app@latest my-app", "cd my-app", "npm install"],
    "dependencies": [{{"name": "react", "version": "19.x"}}, {{"name": "next", "version": "15.x"}}],
    "envVars": [{{"name": "DATABASE_URL", "description": "PostgreSQL connection string", "example": "postgresql://user:pass@localhost:5432/db"}}]
  }},
  "snippets": [
    {{
      "filename": "src/app/page.tsx",
      "language": "typescript",
      "code": "export default async function Home() {{\n  const data = await fetchData();\n  return <div>{{data.title}}</div>;\n}}",
      "explanation": "Server component that fetches data at request time without client-side JavaScript",
      "timestamp": 180
    }}
  ],
  "patterns": [
    {{
      "title": "Data Fetching in Server Components",
      "doExample": "const data = await fetch(url); // Direct async in component",
      "dontExample": "useEffect(() => {{ fetch(url).then(...) }}, []); // Client-side waterfall",
      "explanation": "Server components can use async/await directly, eliminating loading spinners and waterfalls"
    }}
  ],
  "cheatSheet": [
    {{"title": "Create Next.js App", "code": "npx create-next-app@latest", "description": "Scaffold a new Next.js project with TypeScript"}}
  ]
}}

RULES:
- Code must be REAL, RUNNABLE code from the video — not pseudo-code
- Include ALL setup commands in correct order with exact package names and versions
- Code snippets should be complete enough to copy-paste and run
- MINIMUM: 3 snippets, 2 patterns, 3 cheat sheet items
- Preserve exact variable names, function names, class names, and imports
- Quote error messages and console output exactly as shown
- If the tutorial has multiple files, include snippets from each
