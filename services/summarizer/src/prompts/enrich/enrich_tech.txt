<role>
Generate interactive technical learning materials from the extracted content of a tech/coding video.
Think like a senior developer onboarding a teammate — help the viewer understand architecture, remember code patterns and commands, and apply technical concepts in their own projects.
</role>

<video_context>
{video_context}
</video_context>

<extracted_content>
{extraction_data}
</extracted_content>

<instructions>
VIDEO: {title}

Create quiz questions, flashcards, and practical scenarios from the extracted content.
Focus on code patterns, architecture decisions, and technical concepts the viewer can use immediately.

Quiz: Test understanding of technical decisions and patterns from the video.
- Each question has 4 options with exactly 1 correct answer (correctIndex is 0-based)
- Include an explanation for the correct answer (1-2 sentences)
- Good question stems: "What command...", "Why use X instead of Y...", "What happens when...", "Which pattern..."
- Include code-specific questions (function names, package names, flags)

Flashcards: Create review cards for commands, patterns, and architecture.
- Front: technical question (command, pattern, concept, architecture decision)
- Back: exact code, command, or technical explanation from the video
- Include code blocks on the back when relevant
- Cover: setup commands, key code patterns, API endpoints, env vars, architecture decisions

Scenarios: Present real coding decisions the viewer might face.
- Each scenario has 3-4 options with exactly 1 correct
- Good stems: "You need to handle...", "The build fails because...", "You want to add..."
- Test applied technical judgment, not just recall
- Reference specific patterns and anti-patterns from the video
</instructions>

<tab_goals>
{tab_goals}
</tab_goals>

<example>
{{
  "quiz": [
    {{"question": "What command does the speaker use to initialize the project?", "options": ["npm init", "npx create-next-app", "yarn create", "pnpm init"], "correctIndex": 1, "explanation": "At 3:15, the speaker runs npx create-next-app with the --typescript flag"}}
  ],
  "flashcards": [
    {{"front": "What is the purpose of the 'use server' directive?", "back": "Marks a function as a Server Action — it runs on the server when called from client components, enabling database operations without API routes"}}
  ],
  "scenarios": [
    {{"question": "Your Next.js app needs to fetch user data on the server side. Should you use a Server Component or an API route?", "emoji": "🔧", "options": [
      {{"text": "Create an API route and fetch from the client", "correct": false, "explanation": "This adds an unnecessary roundtrip — the server can access the data directly"}},
      {{"text": "Use a Server Component with async/await", "correct": true, "explanation": "Server Components reduce client JavaScript and avoid the API route roundtrip, as the speaker demonstrates at 12:45"}},
      {{"text": "Use getServerSideProps", "correct": false, "explanation": "getServerSideProps is the Pages Router pattern — the App Router uses Server Components instead"}},
      {{"text": "Fetch in useEffect on the client", "correct": false, "explanation": "Client-side fetching means a loading state and extra JavaScript — the server can render it immediately"}}
    ]}}
  ]
}}
</example>

<scaling_rules>
Scale the number of items to the video's depth:
- Short video (under 10 min): 3-4 quiz, 4-6 flashcards, 1-2 scenarios
- Medium video (10-30 min): 5-7 quiz, 6-10 flashcards, 2-3 scenarios
- Long video (30+ min): 8-12 quiz, 10-15 flashcards, 3-5 scenarios
HARD CAPS (do not exceed regardless of video length): max 12 quiz questions, max 15 flashcards, max 6 scenarios.
</scaling_rules>

Return valid JSON with keys: quiz, flashcards, scenarios
