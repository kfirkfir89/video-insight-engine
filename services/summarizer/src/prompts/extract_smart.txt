You are extracting a structured educational summary from a YouTube video transcript. Your goal is to capture the key knowledge so someone can learn the content WITHOUT watching the video.

VIDEO: {title} ({duration_minutes} minutes)

SECTIONS TO POPULATE:
{sections}

ACCURACY RULES:
{accuracy_rules}

TRANSCRIPT:
{transcript}

Extract a comprehensive summary into the JSON structure below.

FIELD GUIDANCE:

"keyPoints" (MINIMUM 5, target 7-10):
- Each point should teach ONE specific thing from the video
- "emoji": Pick a single emoji that represents the point's topic (not generic like 💡)
- "title": Clear, specific title (not "Important Point" but "Redis Caching Reduces DB Load by 90%")
- "detail": 2-3 sentences with SPECIFIC details — numbers, names, quotes, examples from the video
- "timestamp": Approximate video timestamp in seconds where this topic starts
- Cover the full video chronologically — don't cluster all points from the first 5 minutes
- Prioritize insights that are unique to this video, not generic common knowledge

"concepts" (MINIMUM 3, target 5-8):
- Terms, ideas, or frameworks that the viewer should understand after watching
- "name": The term as the speaker uses it
- "definition": Clear definition using the speaker's explanation, not a textbook definition
- "emoji": Relevant emoji for the concept's domain
- Only include concepts that are actually explained or defined in the video

"takeaways" (MINIMUM 3, target 3-5):
- Actionable conclusions someone can apply after watching
- Start each with a verb: "Use...", "Implement...", "Avoid...", "Consider..."
- Be specific: not "Use caching" but "Use Redis for session storage to handle 10x more concurrent users"

"timestamps" (MINIMUM 5, target 8-12):
- Key moments a viewer would want to jump to
- "time": Formatted as "M:SS" or "H:MM:SS"
- "seconds": Same moment as integer seconds
- "label": Clear, descriptive label for what happens at this moment
- Cover: introduction, major topic changes, key demonstrations, conclusions
- Space them roughly evenly through the video, not all clustered at the start

Return JSON only:
{{
  "keyPoints": [
    {{
      "emoji": "🔄",
      "title": "Event-Driven Architecture Decouples Services",
      "detail": "The speaker demonstrates how replacing direct API calls with an event bus reduced inter-service latency from 200ms to 15ms. Services publish events without knowing who consumes them, enabling independent deployment.",
      "timestamp": 120
    }}
  ],
  "concepts": [
    {{
      "name": "Event Sourcing",
      "definition": "Storing every state change as an immutable event rather than overwriting current state. The speaker explains this as 'keeping a ledger instead of just a balance'.",
      "emoji": "📝"
    }}
  ],
  "takeaways": [
    "Use event-driven patterns when services need to communicate but shouldn't be tightly coupled",
    "Start with a simple event bus before considering Kafka — the speaker warns that Kafka adds operational complexity most teams don't need early on"
  ],
  "timestamps": [
    {{"time": "0:00", "seconds": 0, "label": "Introduction and overview of event-driven architecture"}},
    {{"time": "3:45", "seconds": 225, "label": "Live demo: Setting up RabbitMQ event bus"}}
  ]
}}

RULES:
- Use EXACT details from the transcript: specific numbers, names, quotes, examples
- Every keyPoint must teach something SPECIFIC — no filler like "The speaker discusses an important topic"
- Timestamps must be reasonable for the video length (don't put 30:00 in a 10-minute video)
- MINIMUM: 5 keyPoints, 3 concepts, 3 takeaways, 5 timestamps
- Preserve the speaker's terminology and examples — don't paraphrase into generic language
