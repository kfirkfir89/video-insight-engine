fastapi>=0.115.0
uvicorn>=0.27.0
motor>=3.3.0
qdrant-client>=1.10.0
httpx>=0.27.0
sentence-transformers>=2.2.0
litellm>=1.80.0
# LLM observability — no-op when LANGFUSE_PUBLIC_KEY/SECRET_KEY are unset.
langfuse>=2.50.0,<3
pydantic>=2.5.0
pydantic-settings>=2.1.0
structlog>=24.0.0
sse-starlette>=1.6.0
numpy>=1.24.0
cachetools>=5.3.0
# Error tracking (no-op when SENTRY_DSN is unset). Pin to <3 — the 2.x SDK
# ships breaking changes that warrant a dedicated migration pass.
sentry-sdk[fastapi]>=1.40.0,<3

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
