version: '3.8'

# ============================================
# Video Insight Engine - Production
# ============================================

services:
  # ----------------------------------------
  # Infrastructure
  # ----------------------------------------

  vie-mongodb:
    image: mongo:7
    container_name: vie-mongodb
    restart: always
    volumes:
      - vie_mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: video-insight-engine
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASS}
    networks:
      - vie-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 30s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 1G

  # ----------------------------------------
  # Application Services
  # ----------------------------------------

  vie-gateway:
    build:
      context: ./api
      dockerfile: Dockerfile.prod
    container_name: vie-gateway
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      MONGODB_URI: mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASS}@vie-mongodb:27017/video-insight-engine?authSource=admin
      SUMMARIZER_URL: http://vie-summarizer:8000
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
    networks:
      - vie-network
    depends_on:
      vie-mongodb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512M

  vie-summarizer:
    build:
      context: ./services/summarizer
      dockerfile: Dockerfile.prod
    container_name: vie-summarizer
    restart: always
    environment:
      PYTHONUNBUFFERED: 1
      MONGODB_URI: mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASS}@vie-mongodb:27017/video-insight-engine?authSource=admin
      # Multi-provider LLM config
      LLM_PROVIDER: ${LLM_PROVIDER:-anthropic}
      LLM_FAST_PROVIDER: ${LLM_FAST_PROVIDER:-}
      LLM_FALLBACK_PROVIDER: ${LLM_FALLBACK_PROVIDER:-}
      LLM_MODEL: ${LLM_MODEL:-}
      LLM_FAST_MODEL: ${LLM_FAST_MODEL:-}
      LLM_MAX_TOKENS: ${LLM_MAX_TOKENS:-4096}
      LLM_FAST_MAX_TOKENS: ${LLM_FAST_MAX_TOKENS:-2048}
      # Provider API keys
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      LOG_LEVEL: info
    networks:
      - vie-network
    depends_on:
      vie-mongodb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1G

  vie-explainer:
    build:
      context: ./services/explainer
      dockerfile: Dockerfile.prod
    container_name: vie-explainer
    restart: always
    environment:
      PYTHONUNBUFFERED: 1
      PORT: 8001
      MONGODB_URI: mongodb://${MONGO_ROOT_USER}:${MONGO_ROOT_PASS}@vie-mongodb:27017/video-insight-engine?authSource=admin
      # Multi-provider LLM config
      LLM_PROVIDER: ${LLM_PROVIDER:-anthropic}
      LLM_FAST_PROVIDER: ${LLM_FAST_PROVIDER:-}
      LLM_FALLBACK_PROVIDER: ${LLM_FALLBACK_PROVIDER:-}
      LLM_MODEL: ${LLM_MODEL:-}
      LLM_FAST_MODEL: ${LLM_FAST_MODEL:-}
      LLM_MAX_TOKENS: ${LLM_MAX_TOKENS:-4096}
      LLM_FAST_MAX_TOKENS: ${LLM_FAST_MAX_TOKENS:-2048}
      # Provider API keys
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      LOG_LEVEL: info
    networks:
      - vie-network
    depends_on:
      vie-mongodb:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 1G

  vie-frontend:
    build:
      context: ./apps/web
      dockerfile: Dockerfile.prod
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_WS_URL: ${VITE_WS_URL}
    container_name: vie-frontend
    restart: always
    ports:
      - "80:80"
    networks:
      - vie-network
    depends_on:
      - vie-gateway
    deploy:
      resources:
        limits:
          memory: 128M

# ----------------------------------------
# Networks
# ----------------------------------------

networks:
  vie-network:
    driver: bridge
    name: vie-network

# ----------------------------------------
# Volumes
# ----------------------------------------

volumes:
  vie_mongodb_data:
    name: vie_mongodb_data
